services:
  - type: web
    name: smm-elite
    runtime: node
    repo: https://github.com/pastorjohn046-create/boost
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: "3000"
      - key: NODE_ENV
        value: production
